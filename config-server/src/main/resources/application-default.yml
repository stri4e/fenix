server:
  port: 8888
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:3000/eureka
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/config-properties
          searchPaths:
            - '{application}/{profile}'
            - '/global/{profile}'